{
  "name": "victorfateh-app",
  "version": "1.1.0",
  "author": "Victor Fateh",
  "scripts": {
    "build": "parcel build ./*.html",
    "dev": "parcel ./*.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "tailwindcss": "^2.2.19"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.6.0",
    "@parcel/transformer-webmanifest": "^2.6.0",
    "parcel": "^2.6.0"
  }
}
